spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/bank
    username: nghiadev
    password: nghia

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true